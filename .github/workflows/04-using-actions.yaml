name:  "04 - Uso de Actions."
on: 
  - push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Using the Checkout Action
        uses: actions/checkout@v4
      - name: Print
        run: |
          find ./ -print
        
  